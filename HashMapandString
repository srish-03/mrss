 HashMap<Character,Integer> map = new HahshMap<>();
        for(int i=0;i<chars.length();i++)
        {
            if(!map.containsKey(chars.charAt(i))) map.put(chars.charAt(i),1);
            else map.put(chars.charAt(i),map.get(chars.charAt(i))+1);
        }
